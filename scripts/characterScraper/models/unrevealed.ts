import { RegionType } from "./regionType";

export interface Unrevealed {
    birdthday : Date,
    constellation : string,
    region : RegionType,
    affiliation : string,
    dish : string,
    namecard : string,
    obtainType : string,
    obtain: string,
    releaseDate: Date
}